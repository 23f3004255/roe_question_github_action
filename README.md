# Multi-Platform Matrix Build Example

This repository demonstrates a multi-platform matrix build with artifact uploads using GitHub Actions.

Each matrix job builds for a different Node.js version and uploads a unique artifact with the prefix `build-fd210d7`.

**Email:** 23f3004255@ds.study.iitm.ac.in
